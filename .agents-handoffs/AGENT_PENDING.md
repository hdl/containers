THESE NOTES ARE FOR HUMANS ONLY

ponos:
  - are custom version deps also runtime deps or only for building?
  - why is such an ugly PKG_CONFIG_PATH needed?

there is no CLI feature to build all the images for a jobs key

investigate failing xyce

investigate failing verilator
